name: meal_planner
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.13.0
  cached_network_image: ^3.4.1
  firebase_auth: ^5.5.2
  table_calendar: ^3.0.9
  fluttertoast: ^8.2.2
  firebase_storage: ^12.4.5
  image_picker: ^1.0.7
  cloud_firestore: ^5.6.6
  http: ^1.3.0
  intl: ^0.20.2
  url_launcher: ^6.2.5
  provider: ^6.0.0
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.14.3"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/Meal Planner (4).png"
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - "assets/images/Meal Planner (4).png"
